# The core lab intentionally uses only the Python standard library.
# Databricks already includes Python and usually includes MLflow.
# Optional packages for real GEPA / model endpoint runs are in requirements-optional.txt.
